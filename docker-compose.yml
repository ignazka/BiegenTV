services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: localflix
      POSTGRES_PASSWORD: localflix
      POSTGRES_DB: localflix
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
